@page "/fading-entrances"
<link rel="stylesheet" href="css/FadingEntrance.css">

<div class="container">
    <h2>@pageHeader</h2>
    <div class="animation-buttons">
        @foreach (var animation in animations)
        {
            <button class="animation-button" @onclick="() => ToggleAnimation(animation)">@animation</button>
        }
    </div>
    <div class="target @GetAnimationClass()">
    </div>
</div>

@code {
    string pageHeader = "Fading Entrances";

    string currentAnimation = "";

    string GetAnimationClass()
    {
        return currentAnimation == "" ? "" : currentAnimation;
    }

    void ToggleAnimation(string animationName)
    {
        if (currentAnimation == animationName)
        {
            currentAnimation = "";
        }
        else
        {
            currentAnimation = animationName;
        }
    }

    List<string> animations = new List<string> {
        "fadeIn", "fadeInDown", "fadeInDownBig", "fadeInLeft", "fadeInLeftBig", "fadeInRight", "fadeInRightBig", "fadeInUp", "fadeInUpBig"
    };
}